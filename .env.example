# Cache server configuration for local testing with act
# Required when testing GitHub Actions locally, especially on Apple Silicon

ACTIONS_CACHE_URL=http://127.0.0.1:49152/    # URL for local cache server
ACTIONS_RUNTIME_TOKEN=foo                     # Token for cache authentication
ACTIONS_CACHE_DEBUG=true                      # Enable verbose cache logging 